import React from "react";

// Header Component - display the sticky menu
const Header = () => {
    return (
        <div className="ui fixed menu centered grid">
            <div className="ui container centered grid">
                <h2>Workplan Application</h2>
            </div>
        </div>
    );
};

export default Header;